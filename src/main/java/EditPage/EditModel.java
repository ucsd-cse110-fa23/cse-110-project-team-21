package EditPage;

import java.io.IOException;

/*import Main.Main;


public class EditModel {

    private EditController editcontroller;

    public EditModel(EditController controller) {
        this.editcontroller = controller;
    }

    // Update the recipe in the database, whenever the user saved the edited the recipe.
    public void updateRecipe(String editedDescription) {
        try {
            editcontroller.getRecipe().setDescription(editedDescription);
            Main.recipeManager.updateRecipesToDatabase();
        } catch (IOException e1) {
            System.out.println("Could not update recipe");
        }
    }
} */

